<% provide(:title, @user.name) %>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <h2><%= @user.name %></h2>
    <p><%= @user.email %></p>

    <p><%= link_to "編集", edit_user_path(current_user), class: "form-control btn btn-primary" %></p>
  </div>
</div>